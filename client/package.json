{
    "scripts": {
        "build": "tsc",
        "test-client": "ts-node testClient.ts",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "axios": "^1.9.0"
    }
}
